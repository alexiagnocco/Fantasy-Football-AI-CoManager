# Daily Fantasy Report — 2026-08-29

## Preflight failure — no analysis run

The scheduled daily analysis could not run: **none of the required environment
variables are set in this session's environment.**

Missing variables (names only):

- `ESPN_S2`
- `ESPN_SWID`
- `LEAGUE_1_ID`
- `LEAGUE_1_TEAM_ID`

No roster, matchup, start/sit, or waiver analysis was possible without league
credentials, so this report is a status note only.

**Fix**: add these as environment variables/secrets in the Claude Code remote
environment used by this scheduled task (they exist as GitHub Actions secrets,
but those are not visible to this session).

## Environment diagnostics

| Check | Result |
|---|---|
| `ESPN_S2` | missing |
| `ESPN_SWID` | missing |
| `LEAGUE_1_ID` | missing |
| `LEAGUE_1_TEAM_ID` | missing |
| `DISCORD_WEBHOOK_URL` | not set |
| `node --version` | v22.22.2 |
| `python3 --version` | Python 3.11.15 |
| Outbound HTTPS to `lm-api-reads.fantasy.espn.com` | **failed** (connection could not be established through the session's egress proxy — curl exit with no HTTP status) |

Note: even once the environment variables are added, the ESPN API host must be
reachable from this environment (network policy currently appears to block it).
Both issues need addressing before the daily analysis can produce a real report.
